# OpenAI — used for embeddings (text-embedding-3-small) and answer generation (gpt-4o-mini)
OPENAI_API_KEY=sk-...

# Pinecone — vector database for storing and querying code embeddings
PINECONE_API_KEY=pcsk_...
PINECONE_INDEX=legacylens
